# NeuroVision AI — pinned runtime dependencies (V1).
# Pinned exactly for reproducibility (AP-6 / NR-10). Pure NumPy only: the V1
# reference baselines are framework-free and CPU-only so results are
# bit-for-bit reproducible and auditable.
numpy==2.4.6

# Development / verification only (not a runtime dependency of any module):
pytest==9.0.3
