"""Tests for the ``preprocessing/`` DSP foundation (V1-P2).

These encode the layer's guarantees as executable checks: deterministic outputs,
scientifically-correct transforms, report-only quality, and full lineage —
failing the build on violation (see ``tests/README.md``).
"""
