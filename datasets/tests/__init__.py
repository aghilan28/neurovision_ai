"""Tests for the ``datasets/`` EEG data foundation (V1-P1).

These tests encode the data foundation's guarantees as executable checks
(determinism, integrity, traceability, reproducibility) and fail the build on
violation — per the cross-cutting testing contract in ``tests/README.md``.
"""
