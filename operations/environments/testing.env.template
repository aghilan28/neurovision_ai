# NeuroVision AI — TESTING / CI environment template. Contains NO real secrets.
NV_ENV=testing
NV_WORKSPACE_DIR=/tmp/nv_runtime
NV_BACKUP_DIR=/tmp/nv_backups
NV_LOG_LEVEL=warning
NV_LOG_FORMAT=json
NV_METRICS_ENABLED=true
NV_PBKDF2_ITERATIONS=200000
NV_MODEL_ARCHITECTURE=eegnet
# Tests inject a deterministic entropy source in-process; no secrets required.
